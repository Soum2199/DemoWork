def heyTheres(self, arr: List[int], mat: List[List[int]]) -> int:
         current_wins = s.count('W')
    
    	# Calculate remaining matches
    	remaining_matches = 14 - n
    
    # Calculate maximum possible wins
    	max_possible_wins = current_wins + remaining_matches
    
    # Check if it's possible to have at least 8 wins
    	return max_possible_wins >= 8

