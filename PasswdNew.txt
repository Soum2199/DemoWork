<!DOCTYPE html>
<html>
  <head>
    <title>Hello, World!</title>
    <style>
body{
  padding: 15px;
}
.title {
	color: #AC45C6;
}
</style>
  </head>
  <body>
      <h1 class="title">Hello World! </h1>
      <p id="currentTime"></p>
      
      <h2>Password gen</h2>
      <label for="nos">Enter Number:</label><input type="number" id="nos" value="3" min="3" max="67"  />
      <button type="submit" id="submit">Submit.</button>
      <br /> <br />
      <legend>
        <fieldset>Generated Password:  <br />
          <h4></h4>
          </fieldset>
      </legend>
      <script>
function showTime() {
	document.getElementById('currentTime').innerHTML = new Date().toString();
}
showTime();
setInterval(function () {
	showTime();
}, 1000);

const h4 = document.querySelector("h4")
const nos = document.getElementById("nos")
const submit = document.getElementById("submit")

let chars = "1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";

console.log(chars.length)
submit.addEventListener("click", function () {
let valley = Number(nos.value);
let newH4 = ""
// for(let i=1; i<=chars; i++){
for(let i=1; i<=valley; i++){
  // valley += i;
  let myIdx = Math.floor(Math.random()* chars.length)
  newH4 += chars[myIdx]
}
  console.log(newH4)
  h4.innerHTML = newH4;
nos.value = ""
  
})
</script>
  </body>
</html>
---------------------------------------------============= note that ============--------------------------------------------

You're trying to generate a **random password** of a length entered by the user. However, your current logic has a few problems:

### Problems in your JS:

1. `for(let i=1; i<=chars; i++)` is incorrect — you're looping through a **string (`chars`)** as if it's a number.
2. You're appending numbers (`i`) instead of picking **random characters** from `chars`.
3. You're appending to the input value (`valley += i`) instead of building a new random string.
4. You're using `h4.append(valley);` — this will keep adding text each time. Use `textContent` instead to overwrite the old password.

---

### ✅ Here's the **corrected JavaScript**:

```js
const h4 = document.querySelector("h4");
const nos = document.getElementById("nos");
const submit = document.getElementById("submit");

const chars = "1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";

submit.addEventListener("click", function () {
  let length = parseInt(nos.value); // convert input value to number
  let password = "";

  for (let i = 0; i < length; i++) {
    let randomIndex = Math.floor(Math.random() * chars.length);
    password += chars[randomIndex];
  }

  h4.textContent = password; // display the password
  nos.value = ""; // clear input field
});
```

---

### 🔄 No changes needed to your HTML.

But just for clarity, your structure would look like this with better formatting:

```html
<!DOCTYPE html>
<html>
  <head>
    <title>Password Generator</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <h2>Password Generator</h2>
    <label for="nos">Enter Number:</label>
    <input type="number" id="nos" value="3" min="3" max="67" />
    <button type="submit" id="submit">Submit</button>
    
    <br /><br />
    
    <fieldset>
      <legend>Generated Password:</legend>
      <h4></h4>
    </fieldset>

    <script>
      // Insert corrected JS here
    </script>
  </body>
</html>
```

Let me know if you'd like to add options like symbols, uppercase-only, or copy-to-clipboard!
