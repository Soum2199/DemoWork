// today 22nd august 2k25

Got it 👍 I see what you’re trying to do — build a small React app with a **product list**, a **cart**, and navigation. Right now, your `cart` logic is incomplete, so items aren’t actually being passed from **`Produce` → `Cart`**.

Let’s carefully fix this step by step:

---

### 🔑 Problems in your code:

1. `const [cart, setCart] = useState('')`

   * `cart` should be an **array**, not a string, because you want to store multiple products.

   ✅ Use:

   ```js
   const [cart, setCart] = useState([]);
   ```

2. In `carting()`, you’re doing:

   ```js
   setCart(cart)
   ```

   * This doesn’t change anything — it just sets `cart` to itself.
     You should **add a new product** into the `cart`.

   ✅ Use:

   ```js
   function carting(product) {
     setCart([...cart, product]); // push product into array immutably
   }
   ```

3. In `Produce`, you’re not sending the product to `carting`.

   * You should call `carting(i)` when "Add to cart" is clicked.

4. In `Cart`, you’re not displaying cart items. Just `"Cart"`.

---

### 🛠 Fixed Code

#### `App.js`

```jsx
import React, { useState } from 'react';
import { NavLink, Routes, Route } from 'react-router-dom';
import Homepage from './Homepage';
import Produce from './Produce';
import Contact from './Contact';
import Cart from './Cart';

function App() {
  const [cart, setCart] = useState([]);

  function happy() {
    alert('Who needs params');
  }

  // receives product and adds to cart
  function carting(product) {
    setCart([...cart, product]);
  }

  return (
    <>
      <header>
        <nav>
          <ul>
            <li>
              <img
                src="https://www.liblogo.com/img-logo/br988faf1-braun-logo-file-braun-logo-svg-wikimedia-commons.png"
                alt="new logo"
              />
            </li>
            <li><NavLink to="/">home</NavLink></li>
            <li><NavLink to="/products">shop</NavLink></li>
            <li><NavLink to="/contact">contact us</NavLink></li>
            <li><NavLink to="/carts">cart ({cart.length})</NavLink></li>
            <li><button type="button" className="btn btn-primary">Sign in</button></li>
          </ul>
        </nav>
      </header>

      <Routes>
        <Route path="/" element={<Homepage />} />
        <Route path="/contact" element={<Contact />} />
        <Route path="/products" element={<Produce carting={carting} />} />
        <Route path="/carts" element={<Cart cart={cart} />} />
      </Routes>

      <main>
        <button type="button" className="btn btn-primary" onClick={happy}>Primary</button>
      </main>

      <footer>
        <p>&copy; {new Date().getFullYear()} All ri8s literally reserved.</p>
      </footer>
    </>
  );
}

export default App;
```

---

#### `Produce.js`

```jsx
import React from 'react';
import { myData } from "./data";
import { useNavigate } from 'react-router-dom';

function Produce({ carting }) {
  const navigate = useNavigate();

  function buyThem(product) {
    carting(product); // add to cart
    navigate("/carts"); // redirect to cart
  }

  return (
    <>
      <h2 style={{ textAlign: "center", fontSize: "21px" }}>Products</h2>
      <hr />
      <div style={{ justifyContent: 'center', alignItems: 'center' }}>
        {myData.map((i, idx) => (
          <div
            className="each-prdct"
            key={idx}
            style={{
              textAlign: "center",
              fontSize: "21px",
              color: "#13579b",
              border: "2px solid green",
              backgroundColor: "#bbf451",
              margin: "0.75em",
              width: "79vw"
            }}
          >
            <p>{i.names}</p>
            <h5>Rs.{i.price}</h5>
            <p>Description: {i.descript}</p>
            <pre>Rated as: {i.rating}/10</pre>
            <hr />
            <button
              type="button"
              className="btn btn-outline-secondary"
              onClick={() => buyThem(i)}
            >
              Add to cart
            </button>
          </div>
        ))}
      </div>
    </>
  );
}

export default Produce;
```

---

#### `Cart.js`

```jsx
import React from 'react';

function Cart({ cart }) {
  return (
    <div>
      <h2>Cart</h2>
      {cart.length === 0 ? (
        <p>No items in cart.</p>
      ) : (
        <ul>
          {cart.map((item, idx) => (
            <li key={idx}>
              {item.names} - Rs.{item.price}
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}

export default Cart;
```

---

✅ Now the flow is:

* `Produce` → click **Add to cart** → adds product into `App`’s `cart` state → navigates to `/carts`.
* `Cart` page displays items.
* Cart count appears in **navbar** too.

---

Do you want me to also add a **"Remove from cart"** button for each product in `Cart`?
